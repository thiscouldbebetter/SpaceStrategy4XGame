"use strict";
class MapTerrain {
    constructor(name, codeChar, visual, isBlocked) {
        this.name = name;
        this.codeChar = codeChar;
        this.visual = visual;
        this.isBlocked = isBlocked;
    }
}
